name: Build x86_64

on:
  push:
    paths:
      - '_build/_zmod.pyx'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Cython
        run: pip install cython setuptools

      - name: Compile _zmod_x86_64.so
        run: |
          cd _build
          python setup_core.py
          cd ..

      - name: Strip symbols
        run: |
          SO_FILE=$(find _build -name '_zmod*.so' -type f | head -1)
          if [ -n "$SO_FILE" ]; then
            strip --strip-all "$SO_FILE"
          fi

      - name: Move and upload
        run: |
          SO_FILE=$(find _build -name '_zmod*.so' -type f | head -1)
          if [ -n "$SO_FILE" ]; then
            mv "$SO_FILE" _zmod_x86_64.so
            git config user.name "Z-DeployBot"
            git config user.email "deploy@zbot.local"
            git add _zmod_x86_64.so
            git diff --cached --quiet || git commit -m "[skip ci] auto-build"
            git push
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
